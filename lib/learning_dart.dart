library;

export 'src/learning_dart_base.dart';

void main() {
  // final listOfChar = [1, 2, 3, 4, 5, 6, 7];

  // var i = 0;
  // for (var element in listOfChar) {
  //   if (listOfChar[element] % 2 == 0) {
  //     listOfChar[element] *= 2;
  //     print(listOfChar[element]);
  //   }
  // }

  final listOfInt = const [2, 4, 5, 9, 9, 9];
  final fff = listOfInt.asMap();
  print(fff);
}
